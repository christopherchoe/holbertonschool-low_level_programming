#include "holberton.h"

/**
 * add - adds two integers and returns result.
 *
 * @x: first int to add.
 * @y: second int to add.
 * Return: returns the two added integers.
 */
int add(int x, int y)
{
	return (x + y);
}
